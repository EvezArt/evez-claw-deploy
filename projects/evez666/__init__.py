"""evez666 – experimental multi‑agent swarm utilities inspired by the EvezArt ecosystem.

This package ships a tiny orchestration layer that can launch OpenClaw sub‑agents,
coordinate their execution, and aggregate results. It is deliberately lightweight
so it can run inside the same Docker image as evez‑os or as a stand‑alone tool.
"""
